git clone --depth 1 https://git.FreeBSD.org/ports.git /usr/ports && git clone -b releng/15.1 https://git.freebsd.org/src.git /usr/src && cd cd /usr/ports/graphics/drm-latest-kmod && make BATCH=yes install clean && cd /usr/ports/graphics/nvidia-drm-latest-kmod && make BATCH=yes install clean && git clone https://github.com/hyeonkang/freebad15.1.git && cp freebad15.1/rc.conf /etc && cp freebad15.1/loader.conf /boot && rm -rfv /usr/ports && rm /usr/src && reboot

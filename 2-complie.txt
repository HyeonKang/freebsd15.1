#!/bin/sh
git clone --depth 1 https://git.FreeBSD.org/ports.git /usr/ports && git clone -b releng/15.1 https://git.freebsd.org/src.git /usr/src && cd /usr/ports/graphics/drm-latest-kmod && make BATCH=yes install clean && cd /usr/ports/graphics/nvidia-drm-latest-kmod && make BATCH=yes install clean && rm -rfv /usr/ports && rm -rfv /usr/src && reboot
